/**
 * Lightweight feature flags. Flip a value here to enable/disable a feature app-wide.
 */

// Adding competitor accounts (brand Competitors tab + the create-brand wizard).
// Viewing/removing existing competitors is unaffected. Set to `false` to disable
// app-wide.
export const COMPETITOR_ADD_ENABLED: boolean = true
