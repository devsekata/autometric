/**
 * Lightweight feature flags. Flip a value here to enable/disable a feature app-wide.
 */

// Adding competitor accounts is temporarily disabled everywhere (brand Competitors tab
// + the create-brand wizard). Viewing/removing existing competitors is unaffected.
// Set back to `true` to re-enable.
export const COMPETITOR_ADD_ENABLED: boolean = false
